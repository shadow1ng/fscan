id: spark-api-unauth

info:
  name: Spark API Unauthenticated Access
  author: betta
  severity: high
  verified: false
  tags: spark,api,unauth
  created: 2023/07/07

rules:
  r0:
    request:
      method: GET
      path: /v1/submissions
    expression: response.status == 400 && response.body.bcontains(b"Missing an action") && response.body.bcontains(b"serverSparkVersion")
expression: r0()
